name: Setup AutoPkg
description: Install an AutoPkg release package on macOS

inputs:
  version:
    description: AutoPkg release version; omitted resolves the latest stable release
    required: false
    default: ""
  github-token:
    description: GitHub token used to resolve the release
    required: false
    default: ""

outputs:
  release:
    description: Resolved GitHub release tag
    value: ${{ steps.install.outputs.release }}
  version:
    description: Installed AutoPkg version
    value: ${{ steps.install.outputs.version }}

runs:
  using: composite
  steps:
    - name: Install AutoPkg
      id: install
      shell: bash
      env:
        INPUT_VERSION: ${{ inputs.version }}
        GITHUB_TOKEN: ${{ inputs.github-token || github.token }}
      run: bash "$GITHUB_ACTION_PATH/install.sh"
